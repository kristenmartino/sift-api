__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/
.env
venv/
.venv/
.pytest_cache/
.mypy_cache/
*.egg
.DS_Store

# Local scratch / eval artifacts (e.g. scripts/eval_why_it_matters.py --json
# output). May contain prod article text — never committed.
data/_cache/

# OpenSecrets bulk-data files — Phase 3.F.2 (CC NC-SA licensed).
# Re-downloadable from opensecrets.org's bulk-data dashboard; not
# committed to the repo. The importer (`scripts/import_opensecrets_bulk.py`)
# reads from this path locally.
data/opensecrets/
.coverage
coverage.xml
htmlcov/

# unlabeled sampler output — commit the LABELED corpus, not the scratch CSV
# audit_unlinked_entities.py working output. Regenerable, and the
# suggestions file is ~1.2 MB. Dated snapshots ARE committed — see the
# "Audit snapshots" section in scripts/README.md.
data/unlinked_entity_suggestions.csv
data/unlinked_entities_by_category.csv
data/unmatched_search_queries.csv

data/eval/*.labels.csv

# Unlabeled sampler output. Commit the LABELED corpus deliberately (git add -f)
# once event_id is filled in — not the scratch snapshot.
data/eval/clustering_corpus.jsonl
