fastapi==0.141.1
uvicorn==0.52.1
langgraph==1.2.10
anthropic==0.120.2
voyageai==0.5.0
asyncpg==0.31.0
feedparser==6.0.14
pydantic-settings==2.14.2
httpx==0.28.1
eval-type-backport==0.4.0
slowapi==0.1.10
pyyaml==6.0.3
sentry-sdk==2.66.1

# Test and lint tooling lives in requirements-dev.txt — the Dockerfile installs
# only this file, so anything added here ships to the production image.
