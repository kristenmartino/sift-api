fastapi==0.141.1
uvicorn==0.52.1
langgraph==1.2.11
anthropic==0.121.0
# Reaches every OpenAI-compatible provider (OpenAI, Together, Groq, DeepInfra,
# Fireworks) through services/llm_client.py. In requirements.txt rather than
# requirements-dev.txt despite being eval-only today: LLM_MODEL_OVERRIDES can
# route a production stage to one of those models, and a missing import would
# turn a config change into a runtime failure on the ingest path.
openai==2.53.0
voyageai==0.5.0
asyncpg==0.31.0
feedparser==6.0.14
pydantic-settings==2.15.0
httpx==0.28.1
eval-type-backport==0.4.0
slowapi==0.1.10
pyyaml==6.0.3
sentry-sdk==2.67.1

# Test and lint tooling lives in requirements-dev.txt — the Dockerfile installs
# only this file, so anything added here ships to the production image.
